using TaskMaster;
internal class Program
{
    private static void Main(string[] args)
    {
        // testar git commands. (did not update on github after commit, add & push)
        // it does not push the most recent
        // it pushes the changes before tha last and most recent changes
        //FileHandler.Open();
        ApplicationUI.Start();
    }
}
